package ExtraccionVariable;

public class BadMain {

    public class Main {

        void printProductPrice(Order order)) {
            Double totalPrice = order.getPrecio() * order.getCantidad() - order.getOferta() + order.getEnvio() * 2;

            System.out.println(totalPrecio);
        }
    }
}
